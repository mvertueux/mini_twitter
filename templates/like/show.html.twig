{% extends 'base.html.twig' %}

{% block title %}Like{% endblock %}

{% block body %}
    <h1>Like</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ like.id }}</td>
            </tr>
            <tr>
                <th>DateLiker</th>
                <td>{{ like.dateLiker ? like.dateLiker|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_like_index') }}">back to list</a>

    <a href="{{ path('app_like_edit', {'id': like.id}) }}">edit</a>

    {{ include('like/_delete_form.html.twig') }}
{% endblock %}
